import RPi.GPIO as GPIO


# threaded callback for when mag passes over
# add time.time to list of values
# have time intervals

# a.append(time.time())
# for val in a
# if val < cutoff
# a.remove(val)